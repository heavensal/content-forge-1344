<% content_for :title, "Edit review — #{@website.name}" %>

<div class="mx-auto w-full max-w-xl">
  <h1 class="text-2xl font-semibold tracking-tight text-zinc-50">Edit review</h1>

  <div class="<%= content_section_card_classes %> mt-8">
    <%= render "form", review: @review %>
  </div>
</div>
