<% content_for :title, "New website — ContentForge" %>

<div class="mx-auto w-full max-w-xl">
  <h1 class="text-2xl font-semibold tracking-tight text-zinc-50">New website</h1>
  <p class="mt-1 text-sm text-zinc-500">An API token is generated automatically when you save.</p>

  <div class="<%= content_section_card_classes %> mt-8">
    <%= render "form", website: @website %>
  </div>
</div>
